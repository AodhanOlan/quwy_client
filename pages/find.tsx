import * as React from 'react';
import MainLayout from "../layouts/MainLayout";

const Find: React.FC = () => {
    return (
        <MainLayout>
            <div className="find">
                Find
            </div>
        </MainLayout>

    );
}

export default Find;
